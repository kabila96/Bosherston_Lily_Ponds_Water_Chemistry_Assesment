
# Bosherston Lily Ponds Water Chemistry Assesment

Prepared by **Powell A. Ndlovu**, Swansea University — **Chevening Scholar 2025–2026**.

## 📦 Contents
- `app.py` — Streamlit application
- `pond_data.csv` — Input dataset
- `requirements.txt` — Python dependencies
- `report.html` — Static HTML summary (optional for quick sharing)

## ▶️ Quick Start (Local)
```bash
# 1) Create and activate a virtual environment (optional)
python -m venv venv
# Windows
venv\Scripts\activate
# macOS/Linux
source venv/bin/activate

# 2) Install dependencies
pip install -r requirements.txt

# 3) Run Streamlit app
streamlit run app.py
```

The app opens in your browser. Use the left sidebar to filter ponds and variables.

## 🚀 Deploy (Streamlit Community Cloud)
1. Push these files to a GitHub repo.
2. Go to https://share.streamlit.io/ and connect your repo.
3. Set the **main file** to `app.py`.
4. Ensure `pond_data.csv` is in the repo root.

## 📄 Static HTML (Optional)
Open `report.html` for a static, portable summary of key figures (no server required).

## 🧰 Requirements
See `requirements.txt`.
